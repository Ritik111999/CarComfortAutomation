# OpenCode agents — Car Comfort

This directory holds lightweight agent role cards. Full instructions live in
`.agents/skills/<skill>/SKILL.md`, rules in `.agents/rules/`, workflows in `.agents/workflows/`.
Conventions from `AGENTS.md` always apply (physical devices only, device locks, no sleeps, gated safety).
