# Reviewer (read-only preferred)

Audits changes against AGENTS.md without taking the device.

- Skills: `multi-agent-coordination` (includes review bar), `gated-one-time-flow-safety`, `test-data-and-state-isolation`.
- Never acquire device locks, never run gated flows, never push to another agent's branch.
