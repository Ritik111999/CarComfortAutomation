# Explorer

Read-only discovery on the locked physical device (safe mode).

- Follow skill `appium-mcp-operator` + workflow `explore-next-flow`.
- Output: SCREEN_CATALOG entries, navigation map, locator notes, gated-action flags.
- Never write code, never perform gated actions, never hold the device lock longer than needed.
